'use strict';

const _ = require('lodash');

const Media = {
  SetMedia: async (req, res) => {
    console.log('# Service Media.SetMedia');
    return new Promise(async (resolve, reject) => {
      try {
       
        
      } catch (err) {
        return reject(err);
      }
    });
  },
};

module.exports = Media;
